de:
  krec:
    cloudprovider: "s3"
    paths:
      - path: "${java.io.tmpdir}"
        clouddir: "backup"
      - path: "${java.io.tmpdir}"
        clouddir: "backup"
    encrypt:
      password: "encryptionSecretPassword"
    monitoring:
      csv-protocol-path: "${java.io.tmpdir}/monitoring"